package kitbot;

public class KitbotErrorHandler implements ErrorHandler {

    @Override
    public void handleError(float error) {
        // TODO Signal to the navigation thread to adjust based on the PID controller.
    }

}
